
## TODO

### Icon system
- [ ] Mono/colour SVG icon toggle — settings option to switch all toolbar/panel icons
      between monochrome (currentColor, theme-tinted) and full colour versions.
      The view preset icons (XY/XZ/YZ) already use hardcoded axis colours as proof
      of concept. A global `icon_mode = 'mono' | 'colour'` setting in AppSettings
      could gate this across all four workshop tools.

### Model Workshop
- [ ] OBJ export (stub exists in _export_model)
- [ ] Frame/bone tree panel (right side, skeleton hierarchy)
- [ ] Solid/shaded render mode in COL3DViewport for DFF meshes
- [ ] Texture name → TXD Workshop lookup integration
- [ ] DFF write / round-trip save

